<Application x:Class="desktop_app.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:desktop_app"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Style TargetType="Separator" x:Key="MenuSeperator">
                <Setter Property="Width" Value="50" />
                <Setter Property="Height" Value="10" />
            </Style>

            <Style TargetType="Button" x:Key="MenuButton">
                <Setter Property="Background" Value="White" />
                <Setter Property="BorderBrush" Value="White" />
                <Setter Property="Foreground" Value="#FFF8C291" />
            </Style>
            <Style TargetType="Image" x:Key="MenuIcon">
                <Setter Property="Width"  Value="20" />
                <Setter Property="Height"  Value="20" />
            </Style>
            <Style TargetType="TextBlock" x:Key="MenuTextBlock">
                <Setter Property="Width"  Value="150" />
                <Setter Property="Height"  Value="20" />
                <Setter Property="Margin"  Value="10,0,0,0" />
            </Style>
        </ResourceDictionary>
       
    </Application.Resources>
</Application>
